"use client";import {AppShell} from "@/components/AppShell";import {useLocale} from "@/i18n/LocaleContext";
export default function ComingSoon({title}:{title:string}){const {locale}=useLocale();return <AppShell><div className="dashboard"><section className="card emptyState"><span className="eyebrow">MEMGO ENGLISH</span><h1>{title}</h1><p>{locale==="ar"?"هذه الصفحة ضمن المرحلة التالية من البناء. جهزنا المسار حتى ما يظهر خطأ 404 أثناء مراجعة الواجهات.":"This page is part of the next build step. The route is ready so navigation does not lead to a 404 while the interface is being reviewed."}</p></section></div></AppShell>}