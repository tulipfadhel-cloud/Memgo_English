"use server";import {revalidatePath} from "next/cache";import {db} from "@/lib/db";import {currentUser} from "@/lib/auth";export async function toggleActivity(activityId:string){const u=await currentUser();if(!u||u.role!=="STUDENT")throw new Error("Unauthorized");const old=await db.activityCompletion.findUnique({where:{userId_activityId:{userId:u.id,activityId}}});if(old)await db.activityCompletion.delete({where:{id:old.id}});else await db.activityCompletion.create({data:{userId:u.id,activityId}});revalidatePath("/student/plan");revalidatePath("/student")}