"use client";import {useActionState} from "react";import {signIn} from "@/app/actions/auth";import {useLocale} from "@/i18n/LocaleContext";
export function LoginForm(){const {t}=useLocale();const [state,action,pending]=useActionState(signIn,null);return <form action={action} className="authForm"><label>{t.email}<input className="field" name="email" type="email" autoComplete="email" placeholder="name@example.com" required/></label><label>{t.password}<input className="field" name="password" type="password" autoComplete="current-password" placeholder="••••••••" minLength={8} required/></label>{state?.error&&<p className="error" role="alert">{state.error}</p>}<button className="primaryBtn" disabled={pending}>{pending?t.signingIn:t.login}</button></form>}