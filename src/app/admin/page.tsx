import Link from "next/link";import {redirect} from "next/navigation";import {currentUser} from "@/lib/auth";import {db} from "@/lib/db";import {AdminShell} from "@/components/AdminShell";import {Users,CalendarDays,Bell,ArrowLeft,Clock3,CheckCircle2,FilePlus2} from "lucide-react";
export default async function Admin(){const user=await currentUser();if(!user)redirect("/login");if(user.role!=="ADMIN")redirect("/student");const today=new Date();today.setHours(0,0,0,0);const tomorrow=new Date(today);tomorrow.setDate(today.getDate()+1);const [students,plans,unread,todayPlan]=await Promise.all([db.user.count({where:{role:"STUDENT"}}),db.dailyPlan.count(),db.notification.count({where:{readAt:null}}),db.dailyPlan.findFirst({where:{date:{gte:today,lt:tomorrow}},include:{_count:{select:{activities:true}}},orderBy:{date:"desc"}})]);return <AdminShell name={user.name}><main className="adminDashboard"><section className="adminHeading"><div><span className="adminEyebrow">ADMIN DASHBOARD</span><h1>مرحباً، {user.name}</h1><p>نظرة تشغيلية سريعة على منصة MEMGO ENGLISH.</p></div><div className="adminDate">{new Intl.DateTimeFormat("ar-IQ",{weekday:"long",day:"numeric",month:"long",year:"numeric"}).format(new Date())}</div></section><section className="adminStats"><article><span className="adminStatIcon"><Users/></span><div><small>الطلاب المسجلون</small><strong>{students}</strong><em>طالب</em></div></article><article><span className="adminStatIcon"><CalendarDays/></span><div><small>الخطط المنشأة</small><strong>{plans}</strong><em>خطة</em></div></article><article><span className="adminStatIcon"><Bell/></span><div><small>الإشعارات غير المقروءة</small><strong>{unread}</strong><em>إشعار</em></div></article></section><section className="adminDashboardGrid"><article className="adminPanel adminToday"><div className="adminPanelHead"><div><span className="adminEyebrow">TODAY'S PLAN</span><h2>خطة اليوم</h2></div><Link href="/admin/plans">إدارة الخطط <ArrowLeft size={14}/></Link></div>{todayPlan?<div className="adminTodayBody"><div className="adminTodayTitle"><span className={todayPlan.status==="PUBLISHED"?"published":"draft"}>{todayPlan.status==="PUBLISHED"?"منشورة":"مسودة"}</span><h3>{todayPlan.title}</h3></div><div className="adminTodayMeta"><span><FilePlus2 size={15}/>{todayPlan._count.activities} أنشطة</span><span><Clock3 size={15}/>{new Intl.DateTimeFormat("ar-IQ",{day:"numeric",month:"short"}).format(todayPlan.date)}</span></div><Link className="adminOutlineAction" href={"/admin/plans/"+todayPlan.id}>فتح الخطة <ArrowLeft size={15}/></Link></div>:<div className="adminEmpty"><CalendarDays size={24}/><h3>لا توجد خطة لليوم</h3><p>أنشئي خطة اليوم ثم أضيفي الأنشطة قبل النشر للطلاب.</p><Link href="/admin/plans"><FilePlus2 size={15}/>إنشاء خطة</Link></div>}</article><aside className="adminPanel adminActions"><div className="adminPanelHead"><div><span className="adminEyebrow">QUICK ACTIONS</span><h2>إجراءات سريعة</h2></div></div><Link href="/admin/plans"><CalendarDays/><span><b>إدارة الخطة اليومية</b><small>إنشاء، تعديل ونشر الخطط</small></span><ArrowLeft/></Link><div className="adminActionDisabled"><ClipboardIcon/><span><b>الاختبارات والواجبات</b><small>تُفعّل عند اكتمال وحداتها</small></span></div><div className="adminSecurity"><CheckCircle2 size={16}/><span><b>صلاحيات الإدارة مفعّلة</b><small>هذه المنطقة متاحة لحساب ADMIN فقط.</small></span></div></aside></section></main></AdminShell>}
function ClipboardIcon(){return <FileText size={18}/>} 